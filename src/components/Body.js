import React from "react";

import SearchBar from "./SearchBar";
import RestaurantList from "./RestaurantList";

const Body = () => (
  <div className="body">
    <SearchBar />
    <RestaurantList />
  </div>
);

export default Body;
